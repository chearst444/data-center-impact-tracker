// Static newspaper-style site lives in /public (index.html, data-center.html).
// React intentionally renders nothing so the static markup in public/index.html
// is preserved verbatim and remains the source of truth for editors.
function App() {
  return null;
}

export default App;
